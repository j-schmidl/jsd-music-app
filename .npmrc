# vite-plugin-pwa@1.x still declares a peer range that tops out at vite 7, but the
# app runs vite 8 (works fine). Allow the install to proceed so `npm ci` succeeds
# both locally and in CI. Remove this once vite-plugin-pwa widens its peer range.
legacy-peer-deps=true
